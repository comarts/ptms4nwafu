# 第2章 随机变量的分布及其数字特征

## 本章概要
- 随机变量及其分布是定量刻画随机现象的基本形式
- 重点：随机变量概念、概率分布及其表达、常用分布、随机变量函数分布、数字特征
- 难点：随机变量概率分布的理解、随机变量函数分布的计算

## 2.1 随机变量 分布函数
### 随机变量
- 定义2.1.1：设随机试验样本空间为 $\Omega=\{\omega\}$，若对每个样本点 $\omega$ 都有唯一实数 $X=X(\omega)$ 与之对应，且：
  - $X$ 由 $\omega$ 唯一确定
  - 对任意实数 $x$，集合 $\{X\le x\}$ 表示有概率的事件
  - 则称 $X$ 为随机变量
- 理解：取值具有随机性；定义域是样本空间，值域是实数轴
- 解题思路：把随机试验结果数量化，用随机变量表示事件
  - 例：卖出面包数量 $X$，事件“多于150个”表示为 $\{X>150\}$
  - 例：弹着点距离 $X$，事件“0.5 m 到 3 m”表示为 $\{0.5\le X\le 3\}$

### 分布函数
- 定义2.1.2：$F(x)=P\{X\le x\}$ text{(2.1.1)}
- 性质：
  - $0\le F(x)\le 1$
  - $F(-\infty)=\lim_{x\to-\infty}F(x)=0$，$F(+\infty)=\lim_{x\to+\infty}F(x)=1$
  - $F(x)$ 单调不减
  - $F(x)$ 至多可列个间断点，且右连续
- 解题思路：
  - 已知 $F(x)$，求概率：$P\{x_1<X\le x_2\}=F(x_2)-F(x_1)$
  - 分段求分布函数，注意区间端点归属
- 例题思路：
  - 例2.3：离散型分布函数是阶梯函数，右连续
  - 例2.4：由 $F(x)$ 的极限性质求常数 $A$
  - 例2.5：均匀分布的分布函数，分段线性

## 2.2 离散型随机变量及其分布
### 定义与性质
- 定义2.2.1：可能取值为有限个或无穷可列个
- 概率函数（分布列）：$P\{X=x_k\}=p_k,\ k=1,2,\dots$ text{(2.2.1)}
- 性质：
  - 非负性：$p_k\ge 0$
  - 规范性：$\sum_{k=1}^{\infty}p_k=1$
- 分布函数与概率函数关系：$F(x)=P\{X\le x\}=\sum_{x_k\le x}P\{X=x_k\}=\sum_{x_k\le x}p_k$ text{(2.2.3)}

### 常见离散分布
- 等概率分布：$P\{X=x_i\}=\frac{1}{n},\ i=1,2,\dots,n$
- 两点分布：$X\sim(0-1)$，$P\{X=1\}=p,\ P\{X=0\}=1-p$；$E(X)=p$，$D(X)=pq$
- 二项分布：$X\sim B(n,p)$，$P\{X=k\}=C_n^k p^k(1-p)^{n-k},\ k=0,1,\dots,n$ text{(2.2.4)}；$E(X)=np$，$D(X)=npq$
- 泊松分布：$X\sim P(\lambda)$，$P\{X=k\}=\frac{\lambda^k}{k!}e^{-\lambda},\ k=0,1,\dots$ text{(2.2.5)}；$E(X)=\lambda$，$D(X)=\lambda$
- 超几何分布：$X\sim H(n,M,N)$，$P\{X=k\}=\frac{C_M^k C_{N-M}^{n-k}}{C_N^n}$ text{(2.2.6)}；$E(X)=\frac{nM}{N}$，$D(X)=np(1-p)\frac{N-n}{N-1}$，$p=\frac{M}{N}$
- 几何分布：$X\sim G(p)$，$P\{X=k\}=(1-p)^{k-1}p,\ k=1,2,\dots$ text{(2.2.2)}；$E(X)=\frac{1}{p}$，$D(X)=\frac{1-p}{p^2}$

### 例题与思路
- 例2.6：取球模型，求分布列和分布函数；思路：组合计数，分段累计概率
- 例2.7：几何分布，验证规范性；思路：等比级数求和
- 例2.8：由规范性求常数 $C$；思路：$\sum p_i=1$
- 例2.9：二项分布，重复抽样；思路：$n$ 次独立试验，成功次数
- 例2.10：泊松近似；思路：$n$ 大 $p$ 小，$\lambda=np$

## 2.3 连续型随机变量及其分布
### 定义与性质
- 定义2.3.1：若存在非负可积函数 $f(x)$，使 $F(x)=\int_{-\infty}^{x}f(t)dt$ text{(2.3.1)}，称 $X$ 为连续型随机变量，$f(x)$ 为概率密度函数
- 密度函数性质：
  - 非负性：$f(x)\ge 0$
  - 规范性：$\int_{-\infty}^{+\infty}f(x)dx=1$
  - $P\{x_1<X\le x_2\}=\int_{x_1}^{x_2}f(x)dx$
  - 在 $f(x)$ 连续点处，$F'(x)=f(x)$
  - 连续型随机变量取任一点概率为0：$P\{X=x_0\}=0$

### 常见连续分布
- 均匀分布：$X\sim U(a,b)$，$f(x)=\frac{1}{b-a},\ a\le x\le b$ text{(2.3.2)}；$E(X)=\frac{a+b}{2}$，$D(X)=\frac{(b-a)^2}{12}$
- 指数分布：$X\sim e(\lambda)$，$f(x)=\lambda e^{-\lambda x},\ x\ge 0$ text{(2.3.3)}；$F(x)=1-e^{-\lambda x},\ x\ge 0$ text{(2.3.4)}；$E(X)=\frac{1}{\lambda}$，$D(X)=\frac{1}{\lambda^2}$
- 韦布尔分布：$f(x)=\frac{\alpha}{\beta}\left(\frac{x-\delta}{\beta}\right)^{\alpha-1}\exp\left[-\left(\frac{x-\delta}{\beta}\right)^\alpha\right],\ x\ge\delta$ text{(2.3.5)}；参数 $\alpha,\beta,\delta$ 分别表示形状、尺度、位置

### 例题与思路
- 例2.11：由密度函数求系数 $A$、区间概率、分布函数；思路：规范性求 $A$，分段积分求 $F(x)$
- 例2.12：柯西分布；思路：由 $F(-\infty)=0,F(+\infty)=1$ 求 $A,B$，求导得密度
- 例2.13：均匀分布等车时间；思路：长度比即概率
- 例2.14：指数分布寿命；思路：$P\{X>t\}=e^{-\lambda t}$

## 2.4 正态分布
### 正态分布
- 定义2.4.1：$X\sim N(\mu,\sigma^2)$，$f(x)=\frac{1}{\sqrt{2\pi}\sigma}e^{-\frac{(x-\mu)^2}{2\sigma^2}}$ text{(2.4.1)}
- 性质：
  - 关于 $x=\mu$ 对称
  - 最大值在 $x=\mu$，最大值为 $\frac{1}{\sqrt{2\pi}\sigma}$
  - 拐点在 $x=\mu\pm\sigma$
  - $\mu$ 决定位置，$\sigma$ 决定陡峭程度
- 分布函数：$F(x)=\int_{-\infty}^{x}\frac{1}{\sqrt{2\pi}\sigma}e^{-\frac{(t-\mu)^2}{2\sigma^2}}dt$ text{(2.4.2)}

### 标准正态分布
- 定义2.4.2：$X\sim N(0,1)$，$\varphi(x)=\frac{1}{\sqrt{2\pi}}e^{-\frac{x^2}{2}}$ text{(2.4.3)}，$\Phi(x)=\int_{-\infty}^{x}\varphi(t)dt$ text{(2.4.4)}
- 性质：$\varphi(-x)=\varphi(x)$，$\Phi(-x)=1-\Phi(x)$
- 标准化变换：$Y=\frac{X-\mu}{\sigma}\sim N(0,1)$ text{(定理2.4.1)}
- 一般正态概率：$P\{x_1<X\le x_2\}=\Phi\left(\frac{x_2-\mu}{\sigma}\right)-\Phi\left(\frac{x_1-\mu}{\sigma}\right)$
- $3\sigma$ 准则：$P\{|X-\mu|<3\sigma\}\approx 0.9973$

### 分位数
- 双侧分位数 $u_{\alpha/2}$：$P\{|X|>u_{\alpha/2}\}=\alpha$
- 上侧分位数 $u_\alpha$：$P\{X>u_\alpha\}=\alpha$
- 下侧分位数 $-u_\alpha$：$P\{X<-u_\alpha\}=\alpha$

### 例题与思路
- 例2.15：标准正态查表计算
- 例2.16：一般正态标准化后查表
- 例2.17：$P\{|X-\mu|<k\sigma\}=2\Phi(k)-1$
- 例2.18：公共汽车车门高度设计；思路：由 $P\{X>h\}\le 0.5\%$ 求上侧分位数

## 2.5 随机变量函数的分布
### 离散型随机变量函数的分布
- 方法：列出 $Y=g(X)$ 的取值，合并相同值对应的概率
- 例2.19：$X$ 分布列，求 $Y=2X$、$Z=(X-1)^2$ 的分布列

### 连续型随机变量函数的分布
- 分布函数法：
  - $F_Y(y)=P\{Y\le y\}=P\{g(X)\le y\}$
  - 对 $y$ 求导得 $f_Y(y)$
- 例2.20：$X\sim N(0,1)$，$Y=e^X$，得对数正态分布
- 例2.21：$Y=X^2$，$f_Y(y)=\frac{1}{2\sqrt{y}}[f_X(\sqrt{y})+f_X(-\sqrt{y})],\ y>0$
- 定理2.5.1：若 $g(x)$ 严格单调可导，反函数 $x=h(y)$，则
  - $f_Y(y)=f_X[h(y)]|h'(y)|,\ \alpha<y<\beta$ text{(2.5.2)}
- 线性变换：$Y=aX+b$，$f_Y(y)=\frac{1}{|a|}f_X\left(\frac{y-b}{a}\right)$ text{(2.5.1)}
- 例2.23：$X\sim N(\mu,\sigma^2)$，则 $Y=aX+b\sim N(a\mu+b,a^2\sigma^2)$
- 例2.24：$Y=10^X$，求密度

## 2.6 随机变量的数字特征
### 数学期望
- 定义2.6.1：离散型 $E(X)=\sum_{i=1}^{\infty}x_i p_i$ text{(2.6.1)}，要求绝对收敛
- 定义2.6.2：连续型 $E(X)=\int_{-\infty}^{+\infty}x f(x)dx$ text{(2.6.2)}，要求绝对收敛
- 常见分布期望：
  - 两点分布：$p$
  - 二项分布：$np$
  - 泊松分布：$\lambda$
  - 超几何分布：$\frac{nM}{N}$
  - 均匀分布：$\frac{a+b}{2}$
  - 指数分布：$\frac{1}{\lambda}$
  - 正态分布：$\mu$
- 随机变量函数期望：
  - 离散型：$E[g(X)]=\sum_i g(x_i)p_i$ text{(2.6.10)}
  - 连续型：$E[g(X)]=\int_{-\infty}^{+\infty}g(x)f(x)dx$ text{(2.6.11)}
- 期望性质：
  - $E(C)=C$
  - $E(CX)=CE(X)$
  - $E(aX+b)=aE(X)+b$
- 例题思路：
  - 例2.25：比较射手水平；思路：求期望
  - 例2.27：二项分布期望应用
  - 例2.28：由分布列求 $E(2X)$、$E(X^2)$
  - 例2.29：$X\sim N(0,1)$，求 $E(X^2)=1$
  - 例2.30：利用期望性质简化计算

### 中位数
- 定义2.6.3：若 $F(x_0)=\frac{1}{2}$，则 $M_d=x_0$
- 例2.31：离散型中位数
- 例2.32：正态分布中位数为 $\mu$
- 例2.33：柯西分布期望不存在但中位数存在

### 方差
- 定义2.6.4：$D(X)=E[(X-E(X))^2]$ text{(2.6.12)}；标准差 $\sigma(X)=\sqrt{D(X)}$ text{(2.6.13)}
- 计算公式：$D(X)=E(X^2)-[E(X)]^2$ text{(2.6.16)}
- 常见分布方差：
  - 两点分布：$pq$
  - 二项分布：$npq$
  - 泊松分布：$\lambda$
  - 超几何分布：$np(1-p)\frac{N-n}{N-1}$
  - 均匀分布：$\frac{(b-a)^2}{12}$
  - 指数分布：$\frac{1}{\lambda^2}$
  - 正态分布：$\sigma^2$
- 方差性质：
  - $D(C)=0$
  - $D(CX)=C^2D(X)$
  - $D(X+C)=D(X)$
  - $D(X)=0$ 的充要条件是 $P\{X=C\}=1$
- 标准化随机变量：$Y=\frac{X-E(X)}{\sqrt{D(X)}}$，$E(Y)=0,\ D(Y)=1$ text{(2.6.17)}
- 例2.34：标准化变量的期望和方差

### 矩、偏度、峰度
- 原点矩：$\mu_k=E(X^k)$ text{(2.6.18)}；离散 $\sum_i x_i^k p_i$ text{(2.6.19)}；连续 $\int_{-\infty}^{+\infty}x^k f(x)dx$ text{(2.6.20)}
- 中心矩：$v_k=E[(X-E(X))^k]$ text{(2.6.21)}；离散 text{(2.6.22)}；连续 text{(2.6.23)}
- 关系：
  - $v_1=0$
  - $v_2=\mu_2-\mu_1^2$
  - $v_3=\mu_3-3\mu_2\mu_1+2\mu_1^3$
  - $v_4=\mu_4-4\mu_3\mu_1+6\mu_2\mu_1^2-3\mu_1^4$
- 偏度：$sk=\frac{v_3}{\sigma^3}$，描述分布偏斜方向和程度
- 峰度：$k=\frac{v_4}{\sigma^4}-3$，描述与正态分布相比的陡峭程度
- 例2.35：正态分布 $v_3=0$，$v_4=3\sigma^4$
- 例2.36：指数分布原点矩、三阶中心矩

## 小结与常用分布表
- 知识结构：随机变量 → 分布函数 → 离散型/连续型 → 常见分布 → 数字特征
- 常用分布及期望、方差表：
  - 两点分布：$E=p,\ D=pq$
  - 二项分布：$E=np,\ D=npq$
  - 泊松分布：$E=\lambda,\ D=\lambda$
  - 超几何分布：$E=\frac{nM}{N},\ D=np(1-p)\frac{N-n}{N-1}$
  - 均匀分布：$E=\frac{a+b}{2},\ D=\frac{(b-a)^2}{12}$
  - 指数分布：$E=\frac{1}{\lambda},\ D=\frac{1}{\lambda^2}$
  - 正态分布：$E=\mu,\ D=\sigma^2$

## 典型题选讲与习题解题思路
### 例1
- 分段密度函数，求常数 $A$、分布函数、区间概率
- 思路：规范性求 $A$；分段积分求 $F(x)$；用 $F$ 求概率

### 例2
- 正态测量误差，三次独立测量至少一次误差绝对值小于30 m
- 思路：单次概率 $p=P\{-30<X<30\}$；$P(B)=1-(1-p)^3$

### 例3
- $X$ 密度分段，$Y=\sin X$，求密度
- 思路：分布函数法，非单调时分段处理

### 例4
- 混合型随机变量，条件概率与区间长度成正比
- 思路：先求条件概率，再求分布函数

### 例5
- 分段函数 $Y=g(X)$，求分布函数和 $P\{X\le Y\}$
- 思路：根据 $g$ 定义分段求 $Y$ 分布

### 例6
- 判断数学期望是否存在
- 思路：检查 $\sum |x_i|p_i$ 是否收敛，绝对收敛才存在

### 例7
- 取球直到出现白球，求期望与方差
- 思路：写出概率函数，利用级数求期望、方差

### 习题2 解题思路
- 1：袋中7白3黑，不放回，$X$ 首次取到白球时取球次数；思路：离散分布列，分布函数
- 2：$P\{X=k\}=ae^{-k}$，求 $a$；思路：$\sum p_k=1$，等比级数
- 3：两个并联继电器，接通概率0.8；思路：二项分布 $B(2,0.8)$，线路接通概率 $P\{X\ge 1\}$
- 4：400辆出租车，故障率0.02，一天内无故障；思路：二项分布近似泊松 $\lambda=8$，$P\{X=0\}=e^{-8}$
- 5：分段密度函数，求分布函数；思路：分段积分，注意连续性
- 6：分布函数 $F(x)=a+be^{-x^2/2}$；思路：由 $F(\infty)=1,F(0)=0$ 求 $a,b$，求导得密度，积分求概率
- 7：用电量密度，供电不足概率；思路：求 $P\{X>供电量\}$
- 8：5个设备，使用概率0.1；思路：二项分布 $B(5,0.1)$，求各概率
- 9：$X\sim U(-2,2)$，方程有实根概率；思路：判别式 $\Delta\ge0$，转化为 $|X|\ge\frac{4}{3}$
- 10：银行等待时间指数分布；思路：单次离开概率 $p=P\{X>10\}$，$Y\sim B(5,p)$，$P\{Y\ge1\}$
- 11-12：正态分布概率计算；思路：标准化查表
- 13：离散型 $X$，求 $Y=(2X-\pi)^2$、$Y=\cos(2X-\pi)$ 分布列；思路：映射合并概率
- 14：$X\sim N(0,1)$，求 $Y=2X-1$、$Y=e^{-X}$、$Y=X^2$ 密度；思路：单调变换公式或分布函数法
- 15：$X\sim U(0,\pi)$，$Y=2\ln X$ 密度；思路：单调，公式法
- 16：比较两台车床质量；思路：比较期望和方差
- 17：$P\{X=k\}=\frac{a^k}{(1+a)^{k+1}}$，求 $E(X)$；思路：几何分布型，求导或级数
- 18：分段密度，求 $E(X)$；思路：积分
- 19：密度 $f(x)=\frac{1}{\sqrt{\pi}}e^{-x^2+2x-1}$，求 $E(X),D(X)$；思路：配方得正态分布 $N(1,\frac{1}{2})$
- 20：密度 $f(x)=ax+b,\ 1\le x\le2$，$E(X)=\frac{19}{12}$，求 $D(4X+1)$；思路：规范性、期望求 $a,b$，再求方差
- 21：离散型分布列，求 $E(X),E(1-X),E(X^2)$；思路：定义求和
- 22：$X\sim U(0,\pi),Y=\sin X$，求 $E(Y)$；思路：积分 $\int_0^\pi \sin x\cdot\frac{1}{\pi}dx$
- 23：公共汽车发车时间，求候车时间期望；思路：各间隔均匀分布，按间隔加权平均候车时间
- 24：离散型分布列，求中位数 $M_d$；思路：累积概率达到0.5