# 第6章 参数估计（含习题综合解析）

- **参数估计基本概念**
  - 概念定义
    - 总体分布函数 $F(x;\theta)$，其中 $\theta \in \Theta$ 为未知参数
    - 样本 $X_1,X_2,\cdots,X_n$ 的观测值为 $x_1,x_2,\cdots,x_n$
    - 估计量：用于估计 $\theta$ 的统计量 $\hat{\theta}(X_1,X_2,\cdots,X_n)$
    - 估计值：代入样本观测值后的数值 $\hat{\theta}(x_1,x_2,\cdots,x_n)$
  - 分类
    - 点估计 (定值估计)
    - 区间估计 (范围估计)
  - **解题思路**
    - 核心是理解“统计量”与“参数”的区别，统计量不含未知参数。
    - 点估计给出具体数值，区间估计给出置信区间和置信水平 $1-\alpha$。

- **点估计的构造方法 (§6.1)**
  - **矩估计法**
    - 理论基础：大数定律，样本矩依概率收敛于总体矩
    - 步骤：建立总体矩 $E(X^k)$ 与待估参数 $\theta$ 的方程组，用样本矩替换总体矩，解出 $\hat{\theta}$
    - 例：正态总体 $N(\mu,\sigma^2)$ 参数 $\mu$、$\sigma^2$ 的矩估计，$\hat{\mu}=\bar{X}$，$\hat{\sigma}^2=B_2=\frac{1}{n}\sum_{i=1}^{n}(X_i-\bar{X})^2$
    - 例：均匀分布 $U(a,b)$ 参数 $a,b$ 的矩估计，由 $E(X)=\frac{a+b}{2}$ 和 $D(X)=\frac{(b-a)^2}{12}$ 联立解出
    - **习题解析 (题4-7)**
      - 题4：两点分布，求矩估计与MLE，关键是由 $E(X)=p$ 得 $\hat{p}=\bar{X}$；由似然函数求导得 $\hat{p}=\bar{X}$。
      - 题5：均匀分布 $U(0,\beta)$，由 $E(X)=\frac{\beta}{2}$ 令其等于 $\bar{X}$，解得矩估计 $\hat{\beta}=2\bar{X}$。然后求 $E(X^2)$ 和 $D(X)$，代入样本二阶矩。
      - 题6：离散分布，求 $T=\sum a_i N_i$ 的无偏估计，利用 $E(T)=\theta$ 和 $N_i$ 的期望（$N_i \sim B(n, p_i)$）建立方程解出 $a_i$。
      - 题7：指数分布，MLE 为 $\hat{\theta}=\bar{X}$。
  - **最大似然估计法 (MLE)**
    - 思想：挑选使样本观测值出现概率最大的参数值作为估计值
    - 连续型似然函数：$L(\theta) = \prod_{i=1}^{n} f(x_i;\theta)$
    - 离散型似然函数：$L(\theta) = \prod_{i=1}^{n} P(X=x_i;\theta)$
    - 对数似然方程：$\frac{d}{d\theta}\ln L(\theta) = 0$
    - 例：泊松分布 $P(\lambda)$，解似然方程得 $\hat{\lambda}=\bar{X}$
    - 例：正态分布 $N(\mu,\sigma^2)$，解得 $\hat{\mu}=\bar{X}$，$\hat{\sigma}^2=B_2$ (注意此处为有偏估计)
    - 例：均匀分布 $U(a,b)$，似然函数不可导，需根据单调性求最值，得 $\hat{a}=\min(X_i)$，$\hat{b}=\max(X_i)$
    - 性质：最大似然估计的不变性，若 $\hat{\theta}$ 是 $\theta$ 的MLE，则 $g(\hat{\theta})$ 是 $g(\theta)$ 的MLE
    - **习题解析 (题8-10)**
      - 题8：平移指数分布，似然函数 $L(\theta)=e^{-\sum(x_i-\theta)}$，要求 $x_i \ge \theta$，根据单调性，$\hat{\theta}=\min(X_i)$。
      - 题9：几何分布，似然函数求导求解。
      - 题10：幂函数分布，求最大值统计量 $T=\max(X_i)$ 的密度函数，再求 $E(cT)=\theta$ 得到 $c$。
    - **解题思路**：写出似然函数并取对数 $\ln L(\theta)$；对参数求偏导数并令其为0；解方程组得到估计量。若遇不可导函数（如均匀分布），利用似然函数取值0/1的性质和次序统计量求极值。

- **估计量的评价标准 (§6.2)**
  - **无偏性**
    - 定义：$E(\hat{\theta}) = \theta$
    - 系统误差：$E(\hat{\theta}) - \theta = 0$
    - 样本均值 $\bar{X}$ 是总体均值 $\mu$ 的无偏估计
    - 样本方差 $S^2 = \frac{1}{n-1}\sum_{i=1}^{n}(X_i-\bar{X})^2$ 是总体方差 $\sigma^2$ 的无偏估计
    - 统计量 $B_2 = \frac{1}{n}\sum_{i=1}^{n}(X_i-\bar{X})^2$ 不是 $\sigma^2$ 的无偏估计（$E(B_2) = \frac{n-1}{n}\sigma^2$）
    - 有限总体不重复抽样：修正为 $\frac{N}{N-1}S^2$ 才是无偏估计，$E(S^2)=\frac{N}{N-1}\sigma^2$
    - **习题解析 (题13)**
      - 泊松分布 $P(\lambda)$，$\bar{X}$ 是 $\lambda$ 的无偏估计。又因 $E(S^2)=\sigma^2=\lambda$，故 $S^2$ 也是无偏估计。
      - 验证 $\alpha\bar{X}+(1-\alpha)S^2$ 的无偏性，求期望即可。
    - **解题思路**：计算估计量的数学期望，看是否等于待估参数。若涉及有偏估计，可通过乘以适当系数进行修正使其成为无偏估计。
  - **有效性**
    - 定义：若 $D(\hat{\theta}_1) < D(\hat{\theta}_2)$，则称 $\hat{\theta}_1$ 比 $\hat{\theta}_2$ 更有效
    - 在无偏估计的前提下，方差越小越好
    - 例：指数分布中，用 $\bar{X}$ 和 $nZ = n\min(X_1,\cdots,X_n)$ 作为 $\theta$ 的无偏估计，$\bar{X}$ 比 $nZ$ 更有效（当 $n>1$ 时）
    - **习题解析 (题11-12)**
      - 题11：三个估计量均为 $\mu$ 的无偏估计，计算它们的方差，$D(\hat{\mu}_1)=\frac{10}{16}\sigma^2$，$D(\hat{\mu}_2)=\frac{5}{9}\sigma^2$，$D(\hat{\mu}_3)=\frac{1}{2}\sigma^2$，方差最小的最有效。
      - 题12：设 $a\hat{\theta}_1+b\hat{\theta}_2$ 为无偏估计，需满足 $a+b=1$。求 $D(a\hat{\theta}_1+b\hat{\theta}_2)$ 在约束条件下的极小值（拉格朗日乘数法或代入法）。
    - **解题思路**：比较两个无偏估计量的方差大小。方差小的更有效。计算方差时注意利用已知分布的方差公式。
  - **一致性 (相合性)**
    - 定义：对于任意 $\varepsilon > 0$，有 $\lim_{n\to\infty} P(|\hat{\theta}_n - \theta| < \varepsilon) = 1$
    - 即 $\hat{\theta}_n \xrightarrow{P} \theta$
    - **习题解析 (题14)**：证明 $\bar{X}$ 是 $\mu$ 的一致估计量。利用辛钦大数定律，$\bar{X} \xrightarrow{P} \mu$。
    - **解题思路**：通常利用辛钦大数定律或切比雪夫不等式证明。样本矩是总体矩的一致估计量。

- **区间估计 (§6.3)**
  - **基本概念**
    - 置信区间：$[\hat{\theta}_1, \hat{\theta}_2]$
    - 置信水平 (置信度)：$1-\alpha$
    - 双侧置信区间与单侧置信限
    - **习题解析 (题15-16)**
      - 题15：置信区间的正确含义是“反复抽样下，区间包含真值的概率为 $1-\alpha$”，选(C)。
      - 题16：区间长度 $L$ 与 $1-\alpha$ 成正比（置信水平越高，区间越宽），当 $1-\alpha$ 缩小时，$L$ 缩短，选(A)。
    - **解题思路**：找一个含有待估参数 $\theta$ 的样本函数（枢轴量），其分布已知且不依赖于任何未知参数。根据给定的置信水平 $1-\alpha$，确定临界值，将不等式变形，解出 $\theta$ 的范围即为置信区间。
  - **枢轴变量法**
    - 步骤：构造枢轴量 $U$；确定分位数；等价变形求出置信区间
    - 偏好：置信区间长度越短越好，可靠性 $1-\alpha$ 越大越好（两者通常矛盾）

- **正态总体参数的区间估计 (§6.4)**
  - **单个正态总体均值 $\mu$ 的估计**
    - **方差 $\sigma^2$ 已知**：构造 $U = \frac{\bar{X}-\mu}{\sigma/\sqrt{n}} \sim N(0,1)$
      - 置信区间：$\left[\bar{X} - u_{\alpha/2}\frac{\sigma}{\sqrt{n}}, \bar{X} + u_{\alpha/2}\frac{\sigma}{\sqrt{n}}\right]$
    - **方差 $\sigma^2$ 未知**：构造 $T = \frac{\bar{X}-\mu}{S/\sqrt{n}} \sim t(n-1)$
      - 置信区间：$\left[\bar{X} - t_{\alpha/2}(n-1)\frac{S}{\sqrt{n}}, \bar{X} + t_{\alpha/2}(n-1)\frac{S}{\sqrt{n}}\right]$
      - **习题解析 (题21-23, 29(1))**
        - 题21：方差未知，使用 $t$ 分布，选(C)。
        - 题22：已知方差用 $z$ 分布，未知方差用 $t$ 分布。计算 $\bar{x}$ 和 $s$ 后代入。
        - 题23：计算均值、标准差、置信区间、估计精度 $A = 1 - \frac{\Delta}{\bar{x}}$。
        - 题29(1)：抗压强度（正态），求均值的估计和精度。
      - **解题思路**：判断方差是否已知。已知用标准正态分布 $Z$；未知用 $t$ 分布。注意双侧分位数 $u_{\alpha/2}$ 和 $t_{\alpha/2}(n-1)$ 的查找。
  - **单个正态总体方差 $\sigma^2$ 的估计**
    - **均值 $\mu$ 已知**：构造 $\chi^2 = \frac{1}{\sigma^2}\sum_{i=1}^{n}(X_i-\mu)^2 \sim \chi^2(n)$
    - **均值 $\mu$ 未知**：构造 $\chi^2 = \frac{(n-1)S^2}{\sigma^2} \sim \chi^2(n-1)$
      - 置信区间：$\left[\frac{(n-1)S^2}{\chi^2_{\alpha/2}(n-1)}, \frac{(n-1)S^2}{\chi^2_{1-\alpha/2}(n-1)}\right]$
      - **习题解析 (题29(2), 30, 31)**
        - 题29(2)：求标准差 $\sigma$ 的置信区间，等价于求方差 $\sigma^2$ 的置信区间后开方。
        - 题30：求方差 $\sigma^2$ 的置信区间，使用卡方分布。
        - 题31：大样本下标准差估计。利用题目给出的近似分布 $S \sim N(\sigma, \frac{\sigma^2}{2n})$ 构造枢轴量。
      - **解题思路**：注意卡方分布不是对称分布，临界值找分位数时容易混淆，注意区分 $\chi^2_{\alpha/2}$ 和 $\chi^2_{1-\alpha/2}$。
  - **单侧置信限**
    - 均值单侧置信下限：$\bar{X} - u_{\alpha}\frac{\sigma}{\sqrt{n}}$ 或 $\bar{X} - t_{\alpha}(n-1)\frac{S}{\sqrt{n}}$
    - 均值单侧置信上限：$\bar{X} + u_{\alpha}\frac{\sigma}{\sqrt{n}}$ 或 $\bar{X} + t_{\alpha}(n-1)\frac{S}{\sqrt{n}}$

- **两个正态总体参数的区间估计 (§6.5)**
  - **两个正态总体均值差 $\mu_1 - \mu_2$ 的估计**
    - **方差 $\sigma_1^2, \sigma_2^2$ 已知**：构造 $U = \frac{(\bar{X}-\bar{Y}) - (\mu_1-\mu_2)}{\sqrt{\frac{\sigma_1^2}{n_1} + \frac{\sigma_2^2}{n_2}}} \sim N(0,1)$
    - **方差未知但相等 $\sigma_1^2 = \sigma_2^2$**：构造 $T = \frac{(\bar{X}-\bar{Y}) - (\mu_1-\mu_2)}{S_w\sqrt{\frac{1}{n_1} + \frac{1}{n_2}}} \sim t(n_1+n_2-2)$
      - 其中 $S_w^2 = \frac{(n_1-1)S_1^2 + (n_2-1)S_2^2}{n_1+n_2-2}$
      - **习题解析 (题24)**：两正态总体方差相等，求均值差的置信区间。使用合并方差 $S_w^2$ 和 $t$ 分布。
    - **方差未知且不相等 $\sigma_1^2 \neq \sigma_2^2$ (Behrens-Fisher问题)**：使用近似 $t$ 分布，自由度 $f$ 较复杂
    - **解题思路**：先判断方差是否已知，再判断方差是否相等。方差未知但相等时，需用合并方差 $S_w^2$。若题目给出成对数据，需转化为单样本均值问题（$D_i = X_i - Y_i$）。
  - **两个正态总体方差比 $\sigma_1^2 / \sigma_2^2$ 的估计**
    - 构造 $F = \frac{S_1^2 / S_2^2}{\sigma_1^2 / \sigma_2^2} \sim F(n_1-1, n_2-1)$
    - 置信区间：$\left[\frac{S_1^2 / S_2^2}{F_{\alpha/2}(n_1-1, n_2-1)}, \frac{S_1^2 / S_2^2}{F_{1-\alpha/2}(n_1-1, n_2-1)}\right]$
    - **解题思路**：利用 $F$ 分布的分位数性质 $F_{1-\alpha}(n_1, n_2) = \frac{1}{F_{\alpha}(n_2, n_1)}$ 进行转换。

- **大样本下非正态总体参数估计 (§6.6)**
  - **一个两点分布总体 (比例 $p$)**
    - 基于中心极限定理，样本频率 $W = M/n$ 近似服从正态分布
    - 精确公式：解二次不等式 $(1 + \frac{u_{\alpha/2}^2}{n})p^2 - (2W + \frac{u_{\alpha/2}^2}{n})p + W^2 \leq 0$
    - 近似公式：$\left[ W - u_{\alpha/2}\sqrt{\frac{W(1-W)}{n}}, W + u_{\alpha/2}\sqrt{\frac{W(1-W)}{n}} \right]$
    - 泊松分布方法：当 $p$ 很小或很大时，基于泊松分布查表估计
    - **习题解析 (题25, 26, 28, 32)**
      - 题25：松毛虫危害率，随机抽取200株，属于大样本，可用近似公式。
      - 题26：造林成活率，样本量400，大样本，求比例 $p$ 的置信区间。
      - 题28：华山松受害率，抽取80株，样本量较小，需使用“小样本方法”（即解二次不等式的精确公式）。
      - 题32：比较正态分布和泊松分布估计，当 $p$ 较大（500/520）时，用泊松分布估计无效率 $p'=20/520$，再转换回有效率。
    - **解题思路**：题目有“大样本”字眼或 $n$ 很大时，用正态近似。注意区分精确公式和近似公式。若题目给出“不重复抽样”且总体有限，需乘以有限总体修正系数。
  - **两个两点分布总体参数差 $p_1 - p_2$**
    - 大样本下 $W_1 - W_2 \sim N(p_1-p_2, \frac{p_1(1-p_1)}{n_1} + \frac{p_2(1-p_2)}{n_2})$
  - **非正态总体均值估计**
    - 大样本下 $\bar{X}$ 近似服从 $N(\mu, \sigma^2/n)$
    - 方差未知时用 $S$ 代替 $\sigma$
  - **有限总体不重复抽样**
    - 样本均值 $\bar{X}$ 近似服从 $N(\mu, \frac{\sigma^2}{n}\frac{N-n}{N-1})$
    - 引入有限总体修正系数 $\sqrt{\frac{N-n}{N-1}}$
    - **习题解析 (题18, 19)**
      - 题18：林地面积 $500 \text{hm}^2$，抽取50个样地。总体 $N=500$，样本 $n=50$。不重复抽样，需乘以修正系数。估计总蓄积量时，$T = N\bar{X}$。
      - 题19：不重复抽样，总体 $N=1000$，样本 $n=50$。分组数据求均值、方差、置信区间。

- **样本容量的确定 (§6.7)**
  - **正态总体均值估计**
    - 已知置信水平 $1-\alpha$ 和精度 $A_0$，求最小 $n$
    - $n_0 = \left\lceil \left(\frac{t_{\alpha/2}(m-1)s_0}{\bar{x}_0(1-A_0)}\right)^2 \right\rceil + 1$ (需要先做小规模预备调查 $m$)
  - **大样本方法**
    - $n = \max\left\{50, \left\lceil \left(\frac{u_{\alpha/2}s_0}{\bar{x}_0(1-A_0)}\right)^2 \right\rceil + 1\right\}$
    - 已知变异系数 $\nu = \sigma/\mu$ 时：$n = \max\left\{50, \left\lceil \left(\frac{u_{\alpha/2}\nu}{1-A_0}\right)^2 \right\rceil + 1\right\}$
    - **习题解析 (题20, 27)**
      - 题20：给定误差限（$6 \text{m}^3/\text{hm}^2$）和置信度，反解 $n$。注意 $N=500$，样本量 $n$ 相比总体不能忽略时，需考虑有限总体修正系数。题目要求不超过误差限，需代入精确公式或修正公式。
      - 题27：相对误差不超过 $5\%$，即 $A_0=0.95$，可靠度0.95。已知历史成活率 $90\%$（变异系数 $\nu$ 可求），代入大样本公式。
    - **解题思路**：根据误差限公式 $\Delta(\bar{x}) = u_{\alpha/2}\frac{\sigma}{\sqrt{n}}$ 反解 $n$。注意“大样本方法”的公式要求 $n \ge 50$，不足50时取50。所求 $n$ 不为整数时，无论小数部分是多少都直接进位取整（因为样本量只能多不能少）。

- **典型题型与思考题解题思路 (习题与小结)**
  - **题型1：基本概念辨析 (点估计与区间估计)**
    - 思路：点估计给具体值，区间估计给范围与置信度。
  - **题型2：矩估计与极大似然估计的计算 (习题1-10)**
    - 思路：套用公式。矩估计：$\bar{X} = E(X)$。MLE：写出 $L(\theta)$，求导令其为0。
  - **题型3：无偏性、有效性与一致性证明 (习题11-14)**
    - 思路：算期望证无偏，算方差比有效，大数定律证一致。
  - **题型4：区间估计的计算 (习题15-32)**
    - 思路：先辨总体，再辨参数，看方差已知未知，选择 $Z, t, \chi^2, F$ 分布。注意区分双侧和单侧，大样本和小样本。
  - **题型5：样本容量与精度计算 (习题20, 27)**
    - 思路：由 $\Delta$ 公式反推 $n$，注意向上取整。
  - **题型6：点估计精度计算 (习题17, 18, 23, 29(1))**
    - 思路：先计算点估计值 $\hat{\theta}$ 和误差限 $\Delta$，再根据 $A = 1 - \frac{\Delta}{\hat{\theta}}$ 计算精度。
