# 第5章 数理统计的基本概念

## 总体、样本、统计量
### 总体与总体特征数
- 总体与个体
  - 研究对象的全体称为总体（population），组成总体的每个基本单位称为个体。
  - 总体指标：数量指标与属性指标。
- 总体特征数
  - 总体均值：$ \mu = \frac{1}{N} \sum_{i=1}^N x_i = E(X) $ \text{(5.1.1)}
  - 总体方差：$ \sigma^2 = \frac{1}{N} \sum_{i=1}^N (x_i - \mu)^2 = D(X) $ \text{(5.1.2)}
  - 总体标准差：$ \sigma = \sqrt{\sigma^2} $ \text{(5.1.3)}
  - 总体 $k$ 阶原点矩：$ \mu_k = \frac{1}{N} \sum_{i=1}^N x_i^k = E(X^k) $ \text{(5.1.4)}
  - 总体 $k$ 阶中心矩：$ \nu_k = \frac{1}{N} \sum_{i=1}^N (x_i - \mu)^k = E[(X-\mu)^k] $ \text{(5.1.5)}
  - 总体极差：$ R = x_{max} - x_{min} $ \text{(5.1.6)}
  - 总体变异系数：$ v = \frac{\sigma}{\mu} $ \text{(5.1.7)}
  - 总体频率：$ p = \frac{m}{N} $ \text{(5.1.8)}（0-1量化后等价于均值）
- **解题思路（例5.1~5.3）**
  - 明确研究对象，提炼总体指标（如寿命、高度、是否合格）。
  - 理解总体对应随机变量 $X$，总体分布即 $X$ 的概率分布。

### 样本
- 样本与样本值：从总体中按预先设计的方法抽取部分个体，这组个体称为样本，观测值称为样本值。
- 简单随机样本（IID样本）：$X_1, X_2, \cdots, X_n$ 相互独立且与总体同分布。
- 样本的联合分布
  - 离散型：$ P\{X_1=x_1, \cdots, X_n=x_n\} = \prod_{i=1}^n P\{X_i=x_i\} $ \text{(5.1.10)}
  - 连续型：$ f(x_1, \cdots, x_n) = \prod_{i=1}^n f(x_i) $ \text{(5.1.11)}
- **解题思路（例5.4~5.5）**
  - 利用独立性，将总体分布（正态、二项、指数等）的概率密度/分布律直接连乘，得到样本的联合分布。

### 统计量
- 定义：不含任何未知参数的样本函数 $T_n = f(X_1, \cdots, X_n)$。
- 常用统计量
  - 样本均值：$ \bar{X} = \frac{1}{n} \sum_{i=1}^n X_i $ \text{(5.1.12)}
  - 样本方差：$ S^2 = \frac{1}{n-1} \sum_{i=1}^n (X_i - \bar{X})^2 $ \text{(5.1.13)}
  - 样本标准差：$ S = \sqrt{S^2} $ \text{(5.1.14)}
  - 样本 $k$ 阶原点矩：$ A_k = \frac{1}{n} \sum_{i=1}^n X_i^k $ \text{(5.1.15)}
  - 样本 $k$ 阶中心矩：$ B_k = \frac{1}{n} \sum_{i=1}^n (X_i - \bar{X})^k $ \text{(5.1.16)}
  - 样本二阶中心矩与样本方差关系：$ B_2 = \frac{n-1}{n} S^2 $ \text{(5.1.17)}
- 顺序统计量
  - 样本中位数：$ M_e = \begin{cases} X_{(\frac{n+1}{2})}, & n \text{为奇数} \\ \frac{1}{2}(X_{(\frac{n}{2})} + X_{(\frac{n}{2}+1)}), & n \text{为偶数} \end{cases} $ \text{(5.1.18)}
  - 样本极差：$ R = X_{(n)} - X_{(1)} $ \text{(5.1.19)}
  - 样本频率：$ W = \frac{M}{n} $ \text{(5.1.20)}
- **解题思路（例5.6）**
  - 根据定义，将具体数据代入公式计算 $\bar{x}$，$s^2$，$r$。
  - 注意：统计量是随机变量，其观测值是具体数值，常用小写字母表示。

### 样本频率分布及直方图
- 步骤：求极差 -> 确定组距 $d$ -> 确定组限（通常上限排外法） -> 统计频数 $n_i$ 与频率 $f_i$ -> 编制频率分布表。
- 频率密度直方图：以组距 $d$ 为底，高为 $f_i/d$ 的长方形面积代表频率。
- **解题思路（例5.7）**
  - 极差 $R = 27.1 - 16.6 = 10.5$，取组数 $k=6$，组距 $d=2$。
  - 左端点取包含最小值的 16，按 $[a, a+d)$ 区间分组，统计频数。
  - 绘制直方图，所有长方形面积之和为1。

## 抽样分布
### 正态总体的抽样分布
- **定理5.2.1**：设 $X \sim N(\mu, \sigma^2)$，$X_1, \cdots, X_n$ 为简单随机样本，则：
  - (1) 样本均值分布：$ \bar{X} \sim N(\mu, \frac{\sigma^2}{n}) $ \text{(5.2.1)}
  - (2) 样本方差分布：$ \frac{(n-1)S^2}{\sigma^2} \sim \chi^2(n-1) $ \text{(5.2.4)}
  - (3) 独立性：$ \bar{X} $ 与 $ S^2 $ 相互独立。
- **推论与常见分布构造**
  - 单样本 $t$ 统计量：$ T = \frac{\bar{X} - \mu}{S / \sqrt{n}} \sim t(n-1) $（例5.8）
  - 两样本方差比 $F$ 统计量：$ F = \frac{S_1^2 / \sigma_1^2}{S_2^2 / \sigma_2^2} \sim F(n_1-1, n_2-1) $（例5.9）
  - 两样本均值差 $t$ 统计量：$ T = \frac{(\bar{X}-\bar{Y}) - (\mu_1-\mu_2)}{\sqrt{\frac{(n_1-1)S_1^2 + (n_2-1)S_2^2}{n_1+n_2-2}(\frac{1}{n_1}+\frac{1}{n_2})}} \sim t(n_1+n_2-2) $（例5.10）
- **解题思路（典型题选讲）**
  - **例1**：指数分布总体，求联合概率密度函数（连乘），计算统计量期望与方差（利用 $E(X)=1/\lambda, D(X)=1/\lambda^2$ 及期望方差性质）。
  - **例2**：正态总体，求 $E(S^2), D(S^2)$（利用 $\chi^2$ 分布的期望为自由度，方差为2倍自由度），并借助 $\chi^2$ 分布查表计算概率。
  - **例3**：通过独立正态变量的线性组合构造标准正态分布，进而构造 $F$ 分布求概率。
  - **例4**：从正态总体抽样，将样本分为两组构造独立的正态统计量和 $\chi^2$ 统计量，从而拼凑出 $t$ 分布。
  - **例5**：顺序统计量的概率密度函数推导。思想：以 $X_{(k)}$ 落在 $(y, y+\Delta y]$ 内，另有 $k-1$ 个小于 $y$，$n-k$ 个大于 $y+\Delta y$，利用多项分布概率求导得出 $ f_k(y) = n C_{n-1}^{k-1} [F(y)]^{k-1} [1-F(y)]^{n-k} f(y) $。

### 非正态总体样本均值的渐近分布
- **定理5.2.2（中心极限定理）**：总体分布未知，$E(X)=\mu$，$D(X)=\sigma^2$ 存在。当 $n$ 充分大时，$ \bar{X} \stackrel{\text{近似}}{\sim} N(\mu, \frac{\sigma^2}{n}) $ \text{(5.2.5)}
- **解题思路**：遇到总体分布未知或非正态（如二项分布、泊松分布等），但样本量足够大时，直接套用中心极限定理，将样本均值近似视为正态分布进行概率计算。

### 两点分布总体样本频率的渐近分布
- **定理5.2.3**：总体 $X \sim B(1, p)$，样本频率为 $W$。当 $n$ 充分大时，$ W \stackrel{\text{近似}}{\sim} N(p, \frac{p(1-p)}{n}) $ \text{(5.2.6)}
- **解题思路**：样本频数 $M \sim B(n,p)$，根据棣莫弗-拉普拉斯中心极限定理，$M$ 近似服从 $N(np, np(1-p))$。由于 $W=M/n$，根据正态分布线性性质推导出 $W$ 的渐近分布。

## 习题5 解题思路
- **题1~3**：离散/连续总体分布已知，直接利用独立同分布性质求联合分布律或联合概率密度（连乘）。
- **题4**：根据40个具体数据，计算样本均值、方差、极差、中位数。注意计算方差时用 $S^2$ 公式（分母 $n-1$），中位数按大小排序后取中间两个的平均。
- **题5**：辨析抽样分布。标准正态总体的均值 $\bar{X} \sim N(0, 1/n)$，$n\bar{X} \sim N(0,1)$。$\sum X_i^2 \sim \chi^2(n)$。$\frac{\bar{X}}{S/\sqrt{n}} \sim t(n-1)$。需逐项分析。
- **题6**：判断统计量（不含未知参数 $p$）。$X_1+X_2$，$(X_1, X_2, \cdots, X_5)$，$X_3+2p$，$(X_5-X_1)^2$ 中只有含 $p$ 的不是统计量。求 $E(\bar{X}) = p$，$D(\bar{X}) = \frac{p(1-p)}{n}$。
- **题7**：构造 $t$ 分布。已知 $\frac{X}{\sigma^2} \sim \chi^2(n-2)$，$Y \sim N(\mu, \sigma^2)$ 且独立。标准化 $Y$ 得 $\frac{Y-\mu}{\sigma} \sim N(0,1)$。由 $t$ 分布定义，分子标准正态，分母为 $\sqrt{\frac{\chi^2}{n-2}}$，组合得 $\frac{Y-\mu}{\sqrt{X/(n-2)}} \sim t(n-2)$。
- **题8**：查表求分位点（$t, F, \chi^2$ 分布的上侧分位数）。
- **题9**：求 $a, b$ 使二次型服从 $\chi^2$ 分布。思路：将 $X_1-2X_2$ 和 $3X_3-4X_4$ 化为独立的标准正态分布变量。$X_1-2X_2 \sim N(0, 5)$，$3X_3-4X_4 \sim N(0, 25)$。需使 $a \times (X_1-2X_2)^2$ 和 $b \times (3X_3-4X_4)^2$ 化为标准正态平方，即 $a=1/5, b=1/25$，自由度 $k=2$。
- **题10**：已知 $\sum_{i=1}^6 X_i^2 \sim \chi^2(6)$，查表或计算 $P\{\chi^2(6) > 6.54\}$。
- **题11**：两样本结合，构造 $t$ 分布。$X_{n+1} - \bar{X} \sim N(0, \sigma^2(1 + 1/n))$，标准化后与 $S^2$ 构造 $t$ 统计量。需注意样本均值的方差和独立新样本的方差相加。
- **题12**：单样本 $t$ 分布应用。$\frac{\bar{X}-\mu}{S/\sqrt{16}} \sim t(15)$。根据 $P\{|\bar{X}-\mu| > kS\} = 0.95$ 变形求 $k$。
- **题13**：构造 $F$ 分布。$X_1+X_2 \sim N(0, 2\sigma^2)$，$X_1-X_2 \sim N(0, 2\sigma^2)$。标准化后平方，两者独立，比值为 $F(1,1)$。
- **题14**：统计量分布推导。分子 $\sum (X_i - \bar{X})^2 / \sigma^2 \sim \chi^2(n-1)$，分母中 $\sum X_i = n\bar{X} \sim N(0, n\sigma^2)$，故分母部分 $(\sum X_i)^2 / (n\sigma^2) \sim \chi^2(1)$ 且独立，整体构成 $F$ 分布。
- **题15**：根据题4数据，参照例5.7步骤制作频率分布表和直方图。
