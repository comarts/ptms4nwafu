# 第8章 方差分析与回归分析

## 本章提要
- 方差分析：概念与基本思想、单因素方差分析、双因素方差分析、多重比较
- 回归分析：一元线性回归、可线性化的一元非线性回归、一元线性回归方程的应用、多元线性回归
- 重点：方差分析和回归分析的思想、单因素方差分析法、多重比较、一元回归方程的建立与应用
- 难点：统计模型的理解
- **解题思路**：本章需要掌握将实际问题（多组均值差异、变量间依存关系）转化为统计模型，利用离差平方和分解或最小二乘法进行参数估计，并构造相应统计量（F检验或t检验）进行假设检验。

## §8.1 方差分析的概念与基本思想
### 8.1.1 问题的引入
- 多个正态总体均值的比较问题
- 两两t检验的弊端：增加犯第一类错误的概率
- **解题思路**：通过分析试验数据差异的来源（处理效应还是随机误差），提出假设 $H_0: \mu_1 = \mu_2 = \dots = \mu_a$，并构造F统计量进行总体检验，避免多次两两比较带来的误差膨胀。

### 8.1.2 方差分析中的术语
- 试验指标：反映试验效果的特征
- 因素及水平：影响试验指标的原因及因素的不同等级
- 处理：所考察因素的不同水平组合
- 随机误差：不可控因素造成的指标差异
- **解题思路**：明确试验中的指标、因素、水平和处理，确定数据类型，为后续建立统计模型打下基础。

### 8.1.3 方差分析的基本思想
- 差异分解：总变异 = 组间变异（处理效应） + 组内变异（随机误差）
- 比较组间均方与组内均方，若比值显著大于1，则认为因素对指标有显著影响
- **解题思路**：将总离差平方和 $SS_T$ 分解为 $SS_A$ 和 $SS_e$，计算各自的均方 $MS_A$ 和 $MS_e$，若 $F = MS_A / MS_e$ 显著大于临界值，则拒绝原假设，说明因素显著。

### 8.1.4 方差分析的基本假定
- 可加性、正态性、方差齐性
- **解题思路**：在应用方差分析前，必须验证数据是否满足这三个假定。若不满足，常采用平方根、对数等变换使数据满足条件后再进行方差分析。

## §8.2 单因素方差分析
### 8.2.1 单因素等重复试验的方差分析
- **数据结构与模型**：$X_{ij} = \mu + \alpha_i + \varepsilon_{ij}$，假设检验 $H_0: \alpha_1 = \alpha_2 = \dots = \alpha_a = 0$，$\text{(8.2.7)}$
- **参数估计**：最小二乘法，$\hat{\mu} = \bar{X}_{..}$，$\hat{\alpha}_i = \bar{X}_{i.} - \bar{X}_{..}$
- **离差平方和分解**：$SS_T = SS_A + SS_e$，$\text{(8.2.13)}$；自由度 $f_T = ar - 1$，$f_A = a - 1$，$f_e = a(r - 1)$，$\text{(8.2.20)}$
- **假设检验**：$F = MS_A / MS_e \sim F(a-1, a(r-1))$，$\text{(8.2.30)}$
- **多重比较**：LSD法、LSR法（包含SSR法和q法）
- **解题思路**：提出假设 -> 计算各类均值 -> 计算 $SS_T, SS_A, SS_e$ -> 列方差分析表，计算 $F$ 值 -> 查表判断显著性 -> 若显著，进行多重比较。

### 8.2.2 单因素不等重复试验的方差分析
- 重复次数不等时的方差分析模型，需将 $r$ 换成 $r_i$，$\sum \alpha_i = 0$ 改为 $\sum r_i \alpha_i = 0$
- 计算公式相应变化 $\text{(8.2.34)}$
- **解题思路**：与等重复类似，但在计算 $SS_A$ 时采用加权形式，自由度 $f_e$ 变为 $\sum r_i - a$。多重比较的 $LSD$ 和 $LSR$ 公式需考虑不等样本量。

## §8.3 双因素方差分析
### 8.3.1 双因素无重复试验的方差分析
- **统计模型**：$X_{ij} = \mu + \alpha_i + \beta_j + \varepsilon_{ij}$
- **假设检验**：分别检验 $H_{01}: \alpha_i = 0$ 和 $H_{02}: \beta_j = 0$
- **离差平方和分解**：$SS_T = SS_A + SS_B + SS_e$，$\text{(8.3.12)}$；自由度 $f_A = a - 1$，$f_B = b - 1$，$f_e = (a-1)(b-1)$
- **假设检验**：$F_A = MS_A / MS_e$，$F_B = MS_B / MS_e$，$\text{(8.3.22)}$，$\text{(8.3.24)}$
- **解题思路**：建立假设 -> 计算行均值、列均值和总和 -> 分解离差平方和 -> 计算F值 -> 列出方差分析表 -> 分别判断因素A和B的显著性。

### 8.3.2 双因素等重复试验的方差分析
- **统计模型**：$X_{ijk} = \mu + \alpha_i + \beta_j + (\alpha\beta)_{ij} + \varepsilon_{ijk}$
- **假设检验**：检验因素A、因素B以及交互作用A×B的显著性
- **离差平方和分解**：$SS_T = SS_A + SS_B + SS_{A \times B} + SS_e$，$\text{(8.3.36)}$；自由度 $f_A = a - 1$，$f_B = b - 1$，$f_{A \times B} = (a-1)(b-1)$，$f_e = ab(r-1)$
- **假设检验**：$F_A$、$F_B$、$F_{A \times B}$，$\text{(8.3.50)}$
- **多重比较**：若交互作用显著，重点关注交互作用的多重比较；若不显著，则对主效应进行多重比较
- **解题思路**：建立假设 -> 计算各类均值 -> 计算总SS及各项SS -> 计算均方和F值 -> 列方差分析表 -> 判断显著性 -> 若有交互作用显著，进行交互作用的多重比较。

## §8.4 回归分析的基本概念
### 8.4.1 相关关系与回归关系
- 确定性关系 vs 非确定性关系（相关关系）
- 预报因子（自变量 $x$）与预报量（因变量 $Y$）
- **解题思路**：通过散点图观察变量间的关系形态，若存在相关趋势，则可进一步建立回归模型。

### 8.4.2 回归模型与回归方程
- 回归函数：$g(x) = E(Y|x)$，回归模型：$Y = g(x) + \varepsilon$，$\varepsilon \sim N(0, \sigma^2)$，回归方程：$y = g(x)$，$\text{(8.4.2)}$
- **解题思路**：确定因变量和自变量，选择合适的函数形式（如线性）来拟合数据。

### 8.4.3 一元线性回归模型与回归方程
- 模型：$Y = \beta_0 + \beta_1 x + \varepsilon$，$\text{(8.4.3)}$；回归方程：$y = \beta_0 + \beta_1 x$，$\text{(8.4.4)}$
- **解题思路**：确定模型形式后，收集样本数据 $(x_i, Y_i)$，为参数估计做准备。

## §8.5 一元线性回归模型的建立与检验
### 8.5.1 一元线性回归系数 $\beta_0, \beta_1$ 的估计
- 最小二乘法（Least Squares Method），残差平方和最小化 $Q(\hat{\beta}_0, \hat{\beta}_1) = \sum (y_i - \hat{\beta}_0 - \hat{\beta}_1 x_i)^2$，$\text{(8.5.2)}$
- 估计值：$\hat{\beta}_1 = L_{xy} / L_{xx}$，$\hat{\beta}_0 = \bar{y} - \hat{\beta}_1 \bar{x}$，$\text{(8.5.10)}$，经验回归直线过点 $(\bar{x}, \bar{y})$
- **解题思路**：计算样本统计量 $\bar{x}, \bar{y}, L_{xx}, L_{yy}, L_{xy}$ -> 代入公式求 $\hat{\beta}_1, \hat{\beta}_0$ -> 写出经验回归方程。

### 8.5.2 随机误差方差 $\sigma^2$ 的估计
- 估计量：$\hat{\sigma}^2 = Q / (n-2) = \frac{1}{n-2} \sum (Y_i - \hat{Y}_i)^2$，$\text{(8.5.12)}$；$Q = L_{yy} - \hat{\beta}_1 L_{xy}$，$\text{(8.5.13)}$
- **解题思路**：利用离差平方和 $L_{yy}$ 和回归平方和 $\hat{\beta}_1 L_{xy}$ 计算残差平方和 $Q$，再除以自由度 $n-2$ 得到方差估计。

### 8.5.3 最小二乘估计量 $\hat{\beta}_0, \hat{\beta}_1$ 的统计性质
- 无偏性、方差公式，正态分布，预测值 $\hat{y}_0$ 的分布 $\text{(8.5.20)}$
- **解题思路**：理解估计量的抽样分布，为假设检验和区间预测提供理论依据。样本量 $n$ 越大、$x$ 越分散，估计越稳定。

### 8.5.4 一元线性回归的显著性检验
- **F检验**：$H_0: \beta_1 = 0$ vs $H_1: \beta_1 \neq 0$，$F = \frac{U}{Q/(n-2)} \sim F(1, n-2)$，$\text{(8.5.24)}$
- **t检验**：$t = \frac{\hat{\beta}_1}{\sqrt{\hat{\sigma}^2 / L_{xx}}} \sim t(n-2)$，$\text{(8.5.25)}$，F检验与t检验等价，$t^2 = F$
- **样本相关系数检验**：
  - 样本相关系数 $r = \frac{L_{xy}}{\sqrt{L_{xx} L_{yy}}}$，$\text{(8.5.30)}$
  - 临界值 $r_\alpha(n-2) = \frac{t_{\alpha/2}(n-2)}{\sqrt{(n-2) + t_{\alpha/2}^2(n-2)}}$，$\text{(8.5.33)}$
  - 检验步骤：计算 $r_0$ -> 查临界值表 -> 比较判断。
  - $F = \frac{(n-2)r^2}{1-r^2} \sim F(1, n-2)$，$\text{(8.5.31)}$
  - 三种检验方法（F检验、t检验、r检验）等价。
- **解题思路**：提出假设 $H_0: \beta_1 = 0$ -> 计算统计量 $F$、$t$ 或 $r$ -> 查表得到临界值 -> 比较判断是否拒绝原假设 -> 若拒绝，说明回归关系显著，模型有应用价值。

## §8.6 预测、控制与残差分析
### 8.6.1 预测
- **对 $E(Y_0)$ 的估计**：置信区间 $\text{(8.6.2)}$
- **对 $Y_0$ 取值的估计**：预测区间 $\text{(8.6.5)}$，包含随机误差，区间更宽。不能外推。
- **解题思路**：区分预测均值和预测个别值。计算预测值 $\hat{y}_0$，查 $t$ 分布表，代入相应公式计算置信区间或预测区间。

### 8.6.2 控制
- 控制问题是预报的反问题：已知 $Y$ 的范围 $[y_1, y_2]$，求 $x$ 的控制范围 $[x_1, x_2]$
- 解不等式组 $\text{(8.6.7)}$ 或使用近似公式 $\text{(8.6.8)}$
- **解题思路**：根据给定的 $Y$ 目标范围，构建关于 $x$ 的不等式，解出 $x$ 的上下限。注意只有当 $y_2 - y_1$ 足够大时控制区间才有意义。

### 8.6.3 残差分析
- 残差 $e_i = y_i - \hat{y}_i$
- 残差图分析：判断方差齐性、模型选择是否恰当（图8.9和图8.10的解释）
- 异常值判定：残差绝对值比平均值大3~4倍，或超出 $\pm 2\sigma$ 范围
- **解题思路**：绘制残差图，观察散点分布是否呈现随机性。若呈现规律性（如喇叭形），则说明方差不齐或模型选择不当，需考虑数据变换或非线性模型。

## §8.7 可线性化的一元非线性回归
- 基本思想：通过变量代换转化为线性回归
- 常用类型：幂函数、指数函数、S形曲线、对数函数，对应的线性化方法（取对数等）
- 相关指数 $R^2 = 1 - \frac{\sum (y_i - \hat{y}_i)^2}{\sum (y_i - \bar{y})^2}$，$\text{(8.7.1)}$，用于衡量非线性回归的显著性
- **解题思路**：根据散点图选择恰当的曲线类型 -> 进行变量代换转化为一元线性回归 -> 利用最小二乘法求出线性方程 -> 逆变换回原变量 -> 计算相关指数 $R^2$ 判断拟合优度。

## §8.8 多元线性回归
### 8.8.1 一元线性回归的矩阵表示
- $Y = X\beta + \varepsilon$，$E(\varepsilon)=0, D(\varepsilon)=\sigma^2 I_n$
- **解题思路**：用矩阵形式统一表达线性模型，便于推广到多元回归。

### 8.8.2 多元线性回归模型
- $Y = \beta_0 + \beta_1 x_1 + \dots + \beta_p x_p + \varepsilon$
- **解题思路**：确定多个预报因子对预报量的联合影响模型。

### 8.8.3 经验回归方程的建立
- 最小二乘估计：$\hat{\beta} = (X^T X)^{-1} X^T Y$（矩阵形式），或正规方程组 $\text{(8.8.19)}$（代数形式），使用 $L$ 矩阵及克拉默法则求解
- **解题思路**：建立正规方程组，解出回归系数向量 $\hat{\beta}$，得到多元线性回归方程。

### 8.8.4 统计性质
- 无偏性 $E(\hat{\beta}) = \beta$，协方差矩阵 $D(\hat{\beta}) = \sigma^2 (X^T X)^{-1}$
- **解题思路**：理解多元回归估计量的统计性质，为显著性检验提供理论基础。

### 8.8.5 显著性检验
- **整体显著性检验（F检验）**：$H_0: \beta_1=\dots=\beta_p=0$，$F = \frac{U/p}{Q/(n-p-1)} \sim F(p, n-p-1)$，$\text{(8.8.28)}$
- **复相关系数**：$R = \sqrt{1 - Q/L_{yy}}$，$F = \frac{R^2/p}{(1-R^2)/(n-p-1)}$，$\text{(8.8.31)}$
- **单个系数显著性检验（t检验）**：$H_{0j}: \beta_j=0$，$t = \frac{\hat{\beta}_j / \sqrt{c_{jj}}}{\sqrt{Q/(n-p-1)}} \sim t(n-p-1)$，$\text{(8.8.36)}$
- **解题思路**：首先进行F检验判断整体回归是否显著；若显著，再进行t检验逐个判断每个预报因子的重要性，剔除不显著变量（逐步回归思想）。

### 8.8.6 多元非线性回归
- 多项式回归（如 $y=\beta_0+\beta_1 x+\beta_2 x^2$）可以通过变量代换转化为多元线性回归
- **解题思路**：令 $x_1=x, x_2=x^2$，将多项式回归转化为多元线性回归求解。

## 小结与典型题选讲
### 一、本章小结
- 知识结构图：方差分析（单因素、双因素，含多重比较）与回归分析（一元线性、多元线性、非线性转化）
- **解题思路**：建立整体框架，理解各类分析方法的内在联系与适用场景。

### 二、典型题选讲
- 【例1】方差分析基本思想判定问题
- 【例2】无偏估计量证明
- 【例3】单因素完全随机试验计算分布
- 【例4】数据变换后方差分析一致性
- 【例5】单因素不等重复试验（灯丝材料）
- 【例6】双因素无重复试验（火箭燃料）
- 【例7】双因素无重复试验与数据变换（除草剂）
- 【例8】一元线性回归（无截距项）参数估计与分布
- 【例9】水稻播种至齐穗的总积温（预测与控制）
- 【例10】产品废品率与化学成分（多项式回归）
- **解题思路**：通过典型案例，巩固方差分析表计算、多重比较步骤、回归方程建立、预测区间和控制区间求解的流程。

## 习题8
- 概念题、计算题、证明题（涵盖方差分析基本思路、多重比较适用条件、双因素分析F分布证明、一元/多元回归模型建立与检验等）
- **解题思路**：综合运用章节知识，仔细审题，区分方差分析与回归分析，注意公式的适用条件（如等重复/不等重复，有交互/无交互），熟练查表计算。